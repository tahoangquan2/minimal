# Minimal

```powershell
g++ -Ofast -march=native -mtune=native -funroll-loops -fprefetch-loop-arrays -fno-exceptions -fno-rtti main.cpp -o main
```

Behavior:
- `CapsLock` is remapped to `Right Ctrl`.
- Toggle real `CapsLock` with `Left Ctrl + Right Ctrl` (physical right ctrl key).
